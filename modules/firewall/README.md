# module firewall

Azure Firewall **Standard** + Policy + Public IP.

Public IP, Policy (DNS-Proxy), Firewall Standard.

Lab-Regeln: Spoke-to-Spoke, DNS/NTP, Application `example.com`.
`private_ip_ranges = IANAPrivateRanges`. Kein DNAT.
